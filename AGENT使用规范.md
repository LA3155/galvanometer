# AI 智能体 (Agent) 协作与代码审查行为规范

> **目标**：杜绝幻觉、杜绝切片盲区误判、保障用户自主开发节奏、提升代码审查专业度与工程严谨性。  
> **适用范围**：本工程（GD32F303 功耗分析仪）全流程开发交互。

---

## 一、 核心行为约束（最高优先级）

1. **严禁擅自修改项目代码（非经明确要求）**：
   - 除非用户在 Prompt 中**明确指示**修改特定文件，否则 Agent **绝对禁止**调用工具直接修改、创建或覆盖用户的工程源码。
   - 交互模式仅限于：提供技术实现思路、列举参考代码片段、排查编译报错、分析架构优劣。
   - **目的**：避免开发者过度依赖 AI，确保核心代码由开发者自主编写与掌控。

2. **杜绝文件切片盲区与“凭空断定”**：
   - **禁止行为**：禁止仅读取文件首尾几行就草率得出“未实现某函数 / 缺少某代码 / 文件是旧版本”的结论。
   - **强制执行**：在回答用户关于代码存在性、语法结构或逻辑问题前，必须：
     1. 全量扫描目标文件或精确定位对应函数的完整行号范围；
     2. 结合 `grep` 检索全工程符号；
     3. 确认获取到磁盘最新保存内容后再做分析。

---

## 二、 技术审查与分析规范

1. **类型安全与底层细节严格把关**：
   - 区分**指针数组**与**单一标量**（例如：LVGL 的 `lv_color_t *color_p` 像素点阵缓冲区 与 底层 `uint16_t color` 单色填充的区别，严禁强转指针传给单值函数）。
   - 区分**像素数量**与**内存字节数**（RGB565 下每个像素占 2 字节，计算缓冲区长度时需严格校验）。
   - 严格检查基础语法规范（如：`void` 函数禁止 `return` 值、结构体函数指针赋值与宏包装函数的一致性、头文件全局变量的 `extern` 声明等）。

2. **尊重并深入分析现有驱动实现**：
   - 给出调用建议前，必须先读懂用户已有底层驱动的内部逻辑。
   - 示例：若 `lcd_set_window()` 内部已经自动处理了 `LCD_X_OFFSET / LCD_Y_OFFSET`，在中间层和上层调用时绝不能重复增加硬件偏移。

3. **架构解耦与宏隔离原则**：
   - 硬件抽象层（HAL/Middleware）结构体必须绑定带有 `#if HW_USE_xxx` 宏开关的包装函数，不得直接绕过包装层绑定裸驱动，确保裁剪开关真正生效。
   - 区分“业务初始化”与“运行时数据访问”，确保上层任务无需直接包含底层寄存器驱动头文件。

---

## 三、 回答风格与交互准则

1. **实事求是，直面问题**：
   - 出现审查盲区或失误时，立即澄清技术事实，不推诿、不粉饰。
2. **代码片段简洁完整**：
   - 给出的参考示例必须具备完整的上下文与可直接迁移性，突出关键行与修改理由。
3. **结构化与可读性**：
   - 按照“问题原因分析 -> 规范做法与差异对比 -> 参考代码片段 -> 下一步建议”展开，逻辑严密、排版工整。
